version: "2"
sql:
  - engine: "postgresql"
    queries: "backend/queries/platform"
    gen:
      go:
        package: "sqlcgen"
        out: "backend/internal/platform/database/sqlcgen"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_empty_slices: true
