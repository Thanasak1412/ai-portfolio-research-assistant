# Cross-Application Tests

This directory is reserved for future cross-application contract, integration, and end-to-end suites. M0 tests live beside the foundations they verify:

- Go tests under `backend/internal/platform`.
- Frontend unit tests under `apps/web/src`.
- Browser smoke tests under `apps/web/tests/e2e`.

No feature fixtures or business test cases exist during bootstrap.

